# ============================================================
# TaxiPulse — Airflow Container Dependencies
# (Subset of main requirements.txt for the Airflow container)
# ============================================================

# --- Core ---
python-dotenv==1.0.1
requests==2.32.3
pandas==2.1.4
pyarrow==14.0.2
numpy==1.26.4

# --- MinIO (S3-compatible) ---
minio==7.2.12
boto3==1.35.81

# --- PostgreSQL ---
psycopg2-binary==2.9.10
# Note: SQLAlchemy is managed by Airflow — do NOT pin it here

# --- Kafka ---
kafka-python-ng==2.2.3

# --- Data Quality ---
great-expectations==1.3.0

# --- Anomaly Detection ---
scipy==1.12.0

# --- Alerting ---
slack-sdk==3.33.5

# --- Utilities ---
loguru==0.7.3
pyyaml==6.0.2